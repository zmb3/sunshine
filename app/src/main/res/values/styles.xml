<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light">
        <!-- Material Theme only requires us to set a primary color for the status bar -->
        <item name="android:colorPrimary">@color/sunshine_light_blue</item>
        <item name="android:colorPrimaryDark">@color/sunshine_blue</item>
        <item name="android:actionBarStyle">@style/MyActionBarStyle</item>
    </style>

    <style name="MyActionBarStyle" parent="@android:style/Widget.Material.ActionBar.Solid">
        <item name="android:elevation">0dp</item>
    </style>

    <!-- TODO: use logo on MainActivity -->

    <!--<style name="ForecastTheme" parent="AppTheme">-->
        <!--<item name="actionBarStyle">@style/ActionBar.Solid.Sunshine.NoTitle</item>-->
    <!--</style>-->


    <!--<style name="ActionBar.Solid.Sunshine.NoTitle" parent="@style/ActionBar.Solid.Sunshine">-->
        <!--<item name="android:displayOptions">useLogo|showHome</item>-->
        <!--<item name="android:logo">@drawable/ic_logo</item>-->
    <!--</style>-->

    <style name="ForecastListStyle">
        <!-- empty here, because in the default case we don't do anything
             (overridden in values-sw600dp/styles.xml) -->
    </style>

    <style name="WidgetDayStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">8dp</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:orientation">vertical</item>
    </style>

</resources>
